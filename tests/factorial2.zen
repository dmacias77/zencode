# Required Test #
# Factorial (Recursivo) #

function int factorial(int n) {
  set int x;

  if (n = 0)
    x << 1;
  else
    x << factorial(n-1) * n;

  return x;
}

main {
  set int fact, x;

  cwrite << "Número: ";
  cread << x;

  fact << factorial(x);
  cwrite << " ";
  cwrite << "Factorial: ";
  cwrite << fact;

  end
}